import 'package:flutter/material.dart';

class REWallet extends StatelessWidget {
  const REWallet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
