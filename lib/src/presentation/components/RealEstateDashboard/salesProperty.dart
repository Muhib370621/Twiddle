import 'package:flutter/material.dart';

class SalesProperty extends StatelessWidget {
  const SalesProperty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Sales property"),
      ),
    );
  }
}
